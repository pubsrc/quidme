name: Frontend - Dev

on:
  push:
    branches: [dev]
    paths:
      - 'frontend/**'
      - '.github/workflows/frontend-dev.yml'
  workflow_dispatch:

env:
  TF_ROOT: terraform/environments/dev
  AWS_REGION: eu-west-2

concurrency:
  group: frontend-dev
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: dev
    permissions:
      id-token: write
      contents: read
    steps:
      - uses: actions/checkout@v4

      - uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ vars.AWS_ROLE_ARN || secrets.AWS_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      - uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.6.0
          terraform_wrapper: false

      - name: Terraform init
        working-directory: ${{ env.TF_ROOT }}
        run: terraform init

      - name: Load frontend env and deploy targets
        working-directory: ${{ env.TF_ROOT }}
        run: |
          echo "VITE_API_BASE_URL=$(terraform output -raw frontend_vite_api_base_url)" >> "$GITHUB_ENV"
          echo "VITE_COGNITO_USER_POOL_ID=$(terraform output -raw frontend_vite_cognito_user_pool_id)" >> "$GITHUB_ENV"
          echo "VITE_COGNITO_USER_POOL_CLIENT_ID=$(terraform output -raw frontend_vite_cognito_user_pool_client_id)" >> "$GITHUB_ENV"
          echo "VITE_COGNITO_REGION=$(terraform output -raw frontend_vite_cognito_region)" >> "$GITHUB_ENV"
          echo "VITE_COGNITO_OAUTH_DOMAIN=$(terraform output -raw frontend_vite_cognito_oauth_domain)" >> "$GITHUB_ENV"
          echo "VITE_OAUTH_REDIRECT_SIGN_IN=$(terraform output -raw frontend_vite_oauth_redirect_sign_in)" >> "$GITHUB_ENV"
          echo "VITE_OAUTH_REDIRECT_SIGN_OUT=$(terraform output -raw frontend_vite_oauth_redirect_sign_out)" >> "$GITHUB_ENV"
          echo "FRONTEND_BUCKET_NAME=$(terraform output -raw frontend_bucket_name)" >> "$GITHUB_ENV"
          echo "CLOUDFRONT_DISTRIBUTION_ID=$(terraform output -raw frontend_cloudfront_distribution_id)" >> "$GITHUB_ENV"

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Build frontend
        working-directory: frontend
        run: |
          npm ci
          npm run build -- --mode dev

      - name: Upload to S3
        run: aws s3 sync frontend/dist "s3://${FRONTEND_BUCKET_NAME}" --delete

      - name: Invalidate CloudFront
        run: aws cloudfront create-invalidation --distribution-id "${CLOUDFRONT_DISTRIBUTION_ID}" --paths '/*'
